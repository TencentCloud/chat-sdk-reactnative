type: api
group: messageManager
name: setLocalCustomData
features: 设置消息自定义数据
version: 1.1.1
ignore: false
deprecated:
precautions:
  info:
    - 本地保存，不会发送到对端，程序卸载重装后失效
params:
  - type: string
    required: true
    name: messageID
    description: 需要设置的消息id
  - type: string
    required: true
    name: localCustomData
    description: 需要设置的自定义属性
returnExample: |-
    V2TimCallback

    {
        code : number
        desc : string
    }
returnValues:
    - type: number
      name: code
      description: 请求结果[错误码](https://cloud.tencent.com/document/product/269/1671)
    - type: string
      name: desc
      description: 请求结果描述
example: true
