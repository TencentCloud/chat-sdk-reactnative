type: api
group: V2TIMGroupManager
name: deleteGroupAttributes 
features: 删除指定群属性
version: 1.1.1
ignore: false
deprecated:
precautions:
  info:
    - 如果 keys 字段填 null ，则会清空所有的群属性。
    - 目前只支持 AVChatRoom
    - initGroupAttributes、setGroupAttributes、deleteGroupAttributes 接口合并计算， SDK 限制为5秒10次，超过后回调8511错误码；后台限制1秒5次，超过后返回10049错误码
params:
  - type: string
    required: true
    name: groupID
    description: 需要删除属性的群组id 
  - type: string[]
    required: true
    name: keys
    description: 删除的属性key值
returnExample: |-
    V2TimCallback

    {
        code : number
        desc : string
    }
returnValues:
    - type: number
      name: code
      description: 请求结果[错误码](https://cloud.tencent.com/document/product/269/1671)
    - type: string
      name: desc
      description: 请求结果描述符
example: true
