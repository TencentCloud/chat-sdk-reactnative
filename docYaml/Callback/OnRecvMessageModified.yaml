type: callback
group: 
name: OnRecvMessageModified
features: 消息内容被修改
ignore: false
deprecated:
returnExample: |-
  OnRecvMessageModified = void Function(
  V2TimMessage message
  )
returnValues:
  - type: '[V2TimMessage]()'
    name: message
    description: 被修改之后的消息对象
example: true
