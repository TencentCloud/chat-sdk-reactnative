type: callback
group: 
name: OnRecvMessageReadReceipts
features: 消息已读回执通知
version: 1.1.1
ignore: false
deprecated:
precautions:
  info:
    - 如果自己发送的消息支持已读回执，消息接收端调用 [sendMessageReadReceipts]()，自己会收到该通知
returnExample: |-
  OnRecvMessageReadReceipts = void Function(
  V2TimMessageReceipt[] receiptList
  )
returnValues:
  - type: '[V2TimMessageReceipt]()[]'
    name: message
    description: 消息已读列表
example: true
