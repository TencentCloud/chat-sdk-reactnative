type: callback
group: 
name: OnInvitationCancelled
features: 邀请被取消的回调
version: 1.1.1
ignore: false
deprecated:
returnExample: |-
  OnInvitationCancelled = void Function(
  string inviteID,
  string inviter,
  string data
  )
returnValues:
  - type: string
    name: inviteID
    description: 邀请消息信令id
  - type: string
    name: inviter
    description: 邀请人id
  - type: string
    name: data
    description: 邀请被取消时的自定义data
example: true
