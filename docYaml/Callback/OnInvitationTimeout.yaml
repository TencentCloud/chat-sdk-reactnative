type: callback
group: 
name: OnInvitationTimeout
features: 邀请超时的回调
version: 1.1.1
ignore: false
deprecated:
returnExample: |-
  OnInvitationTimeout = void Function(
  string inviteID,
  string[] inviteeList
  )
returnValues:
  - type: string
    name: inviteID
    description: 邀请消息信令id
  - type: string[]
    name: inviteeList
    description: 被邀请人的id列表
example: true
