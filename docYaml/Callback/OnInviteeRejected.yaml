type: callback
group: 
name: OnInviteeRejected
features: 邀请被拒绝的回调
version: 1.1.1
ignore: false
deprecated:
returnExample: |-
  OnInviteeRejected = void Function(
  string inviteID,
  string invitee,
  string data
  )
returnValues:
  - type: string
    name: inviteID
    description: 邀请消息信令id
  - type: string
    name: invitee
    description: 被邀请人id
  - type: string
    name: data
    description: 拒绝邀请时的自定义data
example: true
