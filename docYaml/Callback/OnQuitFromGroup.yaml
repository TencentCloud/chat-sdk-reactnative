type: callback
group: 
name: OnQuitFromGroup
features: 主动退出群组（主要用于多端同步，直播群（AVChatRoom）不支持）
version: 1.1.1
ignore: false
deprecated:
returnExample: |-
  OnQuitFromGroup = void Function(
  string groupID
  )
returnValues:
  - type: string
    name: groupID
    description: 群 ID
example: true
