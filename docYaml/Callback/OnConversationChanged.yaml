type: callback
group: 
name: OnConversationChanged
features: 某些会话的关键信息发生变化
version: 1.1.1
ignore: false
deprecated:
precautions:
  info:
    - 未读计数发生变化、最后一条消息被更新等等，可以根据会话的 orderKey 或 lastMessage 的 timestamp 重新对会话列表做排序。
returnExample: |-
  OnConversationChanged = void Function(
  V2TimConversation[] conversationList
  )
returnValues:
  - type: '[V2TimConversation]()[]'
    name: conversationList
    description: 改变的会话列表
example: true
