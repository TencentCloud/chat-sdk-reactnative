type: callback
group: 
name: OnRecvMessageRevoked
features: 收到消息撤回的通知
version: 1.1.1
ignore: false
deprecated:
precautions:
  info:
    - 收到此回调后，建议更新消息的展示UI，替换为被撤回的状态，不展示消息原始内容。
returnExample: |-
  OnRecvMessageRevoked = void Function(
  string messageid
  )
returnValues:
  - type: string
    name: messageid
    description: 被撤回消息的id
example: true
